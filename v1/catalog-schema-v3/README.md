# Future PRODUCTION_V3 public path

Signed envelope target (M5.2b only):

`current.json`

M5.2a intentionally does **not** create `current.json` here so an ordinary
`git push` cannot publish an unsigned v3 feed to the client endpoint.

Legacy v2 remains:

`../current.json`
